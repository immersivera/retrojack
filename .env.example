# Google Tag Manager container ID. Copy this file to `.env` and set your real ID.
# Tags only fire after the visitor grants consent (Google Consent Mode v2).
# Leave empty to disable GTM entirely.
VITE_GTM_ID=GTM-XXXXXXX
